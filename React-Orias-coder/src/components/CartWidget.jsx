import React from 'react';
import {ShoppingCart} from '@mui/icons-materila';

const CartWidget = () => {
    return (
        <div>
            <ShoppingCart /> 
        </div>
    );
};

export default CartWidget;